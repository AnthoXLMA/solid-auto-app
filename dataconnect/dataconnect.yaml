specVersion: "v1"
serviceId: "solid-auto-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "solid-auto-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
